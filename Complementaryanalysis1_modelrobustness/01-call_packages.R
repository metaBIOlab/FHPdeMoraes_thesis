library(stringi)

library(tidyverse)

library(readxl)

library(modelr)

library(ggpubr)

library(readr)

library(stringr)

library(ggridges)

library(purrr)

library(funModeling)

library(Hmisc)

library(lubridate)

library(ggplot2)

library(ggrepel)

library(magrittr)

library(Rmisc)

library(WRS2)

#library(TeachingDemos)

library(car)

library(effsize)

library(pander)

library(knitr)

library(kableExtra)

library(plotly)

library(sjstats)

library(MASS)

# library(patchwork)

library(broom)

library(pwr)

library(caret)

library(nnet)

library(pROC)

library(ellipse)

library(glmnet) #para computa??o de regress?o penalizada

library(arm) #para o modelo glm bayesiano

library(Epi)

library(ROCR)

library(rstatix)

library(effects)

library(interplot)

library(lsr)

library(multcompView)

library(lsmeans)

library(plotly)

#library(BEST)

library(modelsummary)

library(matlib)

library(compute.es)

library(stargazer)